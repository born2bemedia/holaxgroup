/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['localhost', 'holaxgroup-cms.onrender.com', 'cms.holaxgroup.com'], // Add your image domain here
    },
  };
  
  export default nextConfig;